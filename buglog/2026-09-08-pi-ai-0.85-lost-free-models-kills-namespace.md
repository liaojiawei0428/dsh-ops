---
date: "2026-09-08T11:30:00.000Z"
symptom: "官方仓库更新（09-08 ff 到 c389f96bf3，含 pi-ai 0.84→0.85.1 升级）后，DSH 模型页无法添加任何第三方模型（llm-pi-ai 命名空间消失），存量 pi-ai 路由（unlimitds/bai/agnes/opencode-go/opencode）全部失效：llm/listProviders 只剩 deepseek-official。更新当时三次启动失败（未就绪）被自动隔离误伤 dsh-server-ssh。"
component: "llm-pi-ai / settings 集成（pi-ai 0.85.1 升级回归）"
severity: "critical"
status: "fixed"
root_cause: "settings.yaml 的 llm-pi-ai 段里 opencode provider 的 models 列表含 deepseek-v4-flash-free 与 laguna-s-2.1-free 两个旧模型——pi-ai 0.85.1 的 opencode 目录已删除这两个模型。llm-pi-ai 的 installSection（在 ctx.inject(['settings']) 回调内）解析存储段时抛错 'provider \"opencode\" model \"deepseek-v4-flash-free\" needs an api; the installed catalog does not describe it'。该 inject 回调是独立 fiber，其 rejection 无人等待被静默吞掉（无 unhandledRejection、无日志），导致整个 llm-pi-ai 命名空间从未注册——模型页失去该命名空间（添加按钮无目标），存量路由不注册。进程内复现链：settings+llm+llm-pi-ai 最小挂载 → registrations 空 → 原型追踪 inject 交付 → 拿到上述确切错误。"
fix: "编辑 ~/.dsh/settings.yaml：从 opencode provider 的 models 列表删除 deepseek-v4-flash-free 与 laguna-s-2.1-free（0.85.1 目录已无此二模型，其余 mimo-v2.5-free/nemotron-3-ultra-free/nemotron-3.5-lightning-free 保留）。改前备份至 ~/.dsh/backups/20260908-111120/，yaml 解析后整体原子改写。重启后验证：settings/describe 含 llm-pi-ai 命名空间，resolved providers 5 个全部解析，llm/listProviders 注册 deepseek-official+unlimitds+bai+agnes+opencode-go+opencode，settings/mutate 与 llm/discoverModels 均 OK。附：更新时三次启动失败的自动隔离误伤了 dsh-server-ssh（10:33），dsh-github-push 亦被摘出 bundles——两者与本次根因无关，修复后应重新加入 bundles 恢复。"
related_files:
  - "C:\\Users\\Administrator\\.dsh\\settings.yaml"
  - "E:\\DSH\\Deepseek_DSH\\packages\\llm\\llm-pi-ai\\src\\index.ts"
---

排查路径：settings/describe 对比 llm-deepseek（正常注册）与 llm-pi-ai（缺失）→ 直接 RPC 复现（settings/mutate 报 namespace not registered、llm/listProviders 只剩 deepseek-official、listConfigurableProviders 无用户声明路由）→ 存储段单独过 Config schema 通过（段本身格式没问题）→ 进程内最小挂载复现（fiber 静默失败）→ 原型追踪 ctx.inject 交付捕获确切错误。上游观察：inject 回调失败无任何日志且不阻断启动，此类"段解析失败杀死整个命名空间"的问题在 UI 上无诊断，只能从控制台/RPC 层发现；上游可考虑把 installSection 失败路径落到日志或返回可读状态。settings-file 热重载只刷新已注册命名空间的值，命名空间注册只在启动时发生——修复存储段后必须重启。恢复 dsh-server-ssh/dsh-github-push：把名字加回 ~/.dsh/profiles/web/package.json 的 dsh.profile.bundles 后重启（闸门会重新校验）。