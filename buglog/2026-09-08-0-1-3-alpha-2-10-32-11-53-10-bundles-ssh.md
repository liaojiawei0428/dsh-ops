---
date: "2026-09-08T06:00:29.967Z"
symptom: "0.1.3-alpha.2 更新后服务\"就绪即崩溃\"连锁（10:32-11:53 崩溃+误隔离交替），10 插件反复被摘出 bundles，SSH/GitHub 面板不可用"
component: "DSH-ops 看门狗 G5（watchdog-dsh.ps1）+ 上游 connection 回归联动"
severity: "major"
status: "fixed"
root_cause: "0.1.3-alpha.2 上游 connection 回归：inject 懒注入改造漏改 registerFetchRoute 内 owner.webServer 访问 → rpc.handle 路径崩溃 → 自研 server-ssh/github-push 启动即崩 → 次生看门狗误隔离。"
fix: "上游回归由 rpc-host.ts 单行修复解决（owner.root.webServer）；看门狗误隔离由 watchdog-dsh.ps1 重启标记豁免 + 错误特征过滤修复。两者均已实证有效。"
related_files:
  - "E:\\DSH\\Deepseek_DSH\\packages\\client\\connection\\src\\rpc-host.ts"
  - "E:\\DSH\\DSH-ops\\watchdog-dsh.ps1"
---

补充证据关联：本机 10:32-11:53 系列"服务就绪即崩溃"的真实根因是上游回归（0.1.3-alpha.2 connection 插件 inject 从 ['webServer','credentials'] 改为 ['credentials']，rpc-host.ts registerFetchRoute 内仍访问 owner.webServer → Cordis 守卫抛 cannot get property "webServer" without inject；全仓库唯一生产使用 rpc.handle 的是自研 dsh-server-ssh/dsh-github-push，启动即崩；api-gateway 用 rpc.intercept 不踩雷）。修复：packages/client/connection/src/rpc-host.ts 单行 owner.webServer → owner.root.webServer（lib 为 gitignore 构建产物，下次全量 build 自带）。本机验证：服务 13284 稳定、err.log 空、10 插件全载、/dsh-server-ssh 通道 200、settings/llm 命名空间正常。看门狗误隔离（request-restart-err-log-10-32-dsh-github）是此上游崩溃的次生现象：服务崩→看门狗从 err.log 旧痕迹误定位健康插件→连锁摘出 bundles。watchdog-dsh.ps1 已加"主动重启窗口豁免"（检测 restart-resume.json 新鲜标记跳过隔离）+ Get-BrokenPluginName 错误特征过滤；11:43:42 日志实证豁免生效（"检测到 restart-resume 续聊标记 (主动重启窗口), 跳过肇事插件隔离"）。上游修复前需保留 src/rpc-host.ts 的 1 行补丁（update-dsh 工作树干净检查会拦，更新后需重打）。


> 2026-09-09 架构分离更新: 补丁载体已从官方 checkout（现已纯净）迁移到个人运行副本 `E:\DSH\DSH-ops\Deepseek_DSH`，由 `official-patches/apply-patches.mjs` 精确文本替换管理（见 ARCHITECTURE.md）。官方升级后由 sync-official.ps1 自动重打。
